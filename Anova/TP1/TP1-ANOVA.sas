* Exo 1 

  Unités expérimentales : enfants
  Réponse : croissance de l'enfant
  Traitements : régime A et B
  Expérience controlée : oui les traitements sont assignés aux unités expérimentales
  Je déconseillerais les méthodes A. car les enfants vont probablement choisir le chocolat en plus grande quantité
  et B. car si ils sont groupés par personnes semblables...;

Data A ;
do i=1 to 19;
X= Ranuni(1);
output;

end;
Output ;
run;
proc sort data=A;
by X;
run;

Data B ; 

Input Enfant$ Traitement$ @@;
Datalines;
E1 A E2 A E3 A E4 A E5 A E6 A E7 A E8 A E9 A E10 A E11 B E12 B E13 B E14 B E15 B E16 B E17 B E18 B E19 B E20 B
;
run;

Data Fin ;
merge A B;
drop X i;
run;
proc sort data=fin;
by Enfant;
run;
proc print ;
run;

*Exo 1 par le prof;
Data K ;
 Do I=1 to 20;
 Enfant = "E" !! put(I,2.);
 output;
 end;
 run;

Data K ;
Set K;
Ordre = ranuni(999);
run;
Proc sort ;
by ordre ;
run;
Data K ;
set K;
IF _N_ LE 10 then regime = 'A';
ELSE regime = 'B';
Drop I ordre;
run;

Proc sort ;
by enfant;
run;

*exo 2 ; 
Data R ;
 Input Regime poids @@ ;
  Datalines;
  1 3.52 2 3.47 3 3.54 4 3.74 1 3.36 2 3.73 3 3.53 4 3.83
  1 3.57 2 3.38 3 3.61 4 3.87 1 4.19 2 3.87 3 3.76 4 4.08
  1 3.88 2 3.69 3 3.65 4 4.31 1 3.76 2 3.51 3 3.51 4 3.98
  1 3.94 2 3.35 4 3.86 2 3.64 4 3.71
  ;
  run;
  proc sort ; 
  by regime;
  run;
proc sgplot ;
Vbox poids / category = regime;
run;

proc ttest ;
where  regime=1 or regime=2;
class regime;
var  poids;
run;
* p-value = .2 alors on ne rejette pas H0;

proc ttest ; 
where regime=1 or regime =3;
class regime ;
var poids;
run;
* p-value = .25 alors on ne rejette pas H0;

proc ttest;
where regime =1 or regime =4;
class regime ; 
var poids ;
run;
*p-value = .18 alors on rerejette pas H0;

proc ttest ;
where regime=2 or regime=3;
class regime;
var poids;
run;
*p-value =. 85 rejette pas H0;
proc ttest ;
where regime=2 or regime=4;
class regime;
var poids;
run;
* p-value = .0028 alors on rejette H0;
proc ttest ;
where regime=3 or regime=4;
class regime;
var poids;
run;
*p-value = .0032 alors rejette H0;



















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































